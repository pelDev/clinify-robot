{
  "name": "clinify-robot-backend",
  "version": "0.0.1",
  "description": "",
  "author": "Pelumi",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "setup": "yarn && cd backend && yarn && cd .. && cd frontend && yarn",
    "start-backend": "cd backend && yarn start",
    "start-frontend": "cd frontend && yarn start",
    "build-frontend": "cd frontend && yarn build",
    "dev": "concurrently \"yarn start-backend\" \"yarn start-frontend\"",
    "start": "yarn start-backend",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false cd frontend && yarn build"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
